/* eslint-disable no-unused-vars */

export enum TransactionStatus {
  PENDING = 'Pending',
  EXECUTED = 'Executed',
  REJECTED = 'Rejected'
}

export enum Screens {
  DASHBOARD = 'DASHBOARD',
  TRANSACTIONS = 'TRANSACTIONS',
  ASSETS = 'ASSETS',
  POLICY = 'POLICY'
}
